apiVersion: 1

datasources:
  - name: Prometheus
    # 대시보드 JSON 이 이 uid 를 그대로 참조한다. 바꾸면 패널이 전부 깨진다.
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
