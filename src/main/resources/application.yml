spring:
  application:
    name: credit_system
  datasource:
    url: jdbc:mysql://localhost:3306/credit_system_k
    username: root
    password: an902318
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  task:
    scheduling:
      pool:
        size: 4

app:
  scheduling:
    enabled: true
  worker:
    enabled: true
    batch-size: 3
  generation:
    cost: 100
  stub:
    failure-rate: 0.3
    min-delay-millis: 3000
    max-delay-millis: 7000

logging:
  level:
    com.example.credit_system_kotlin: info
